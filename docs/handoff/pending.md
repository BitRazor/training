# pending.md — not yet done (training app)

`[USER ACTION]` Optional: supply a smaller backward-sled gif if the 8.6 MB load feels slow (see `issues.md`).

**Polish offered, not built (user to pick):**
- Offline / installable **PWA** ("Add to Home Screen"; works with no gym signal). Highest practical value.
- **Auto-start the rest timer when ✓ is tapped** (optional toggle).
- **Visual tempo pulse** (metronome flashes so you can follow 3·3 without staring).
- **"Next exercise" jump** button.
- **Plate calculator** (target kg → plates per side).

**Future (separate app):** build `tracker.html` — the ongoing post-calibration tracker. Spec: `C:\Users\arnol\Documents\Claude\Projects\Training\tracking\app-spec.md` (NOT built).
