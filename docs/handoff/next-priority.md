# next-priority.md

The Week-1 calibration app is **feature-complete and fully illustrated** (all 15 exercises + both days' warm-ups have an image). **No blocking work.**

If no new request next session:
1. Optional: shrink `gifs/wu_sled.gif` (**8.6 MB** — heavy on mobile data). See `issues.md`.
2. Optional polish offered but not built — see `pending.md` (offline/PWA install, auto-start rest on ✓, visual tempo pulse, next-exercise jump, plate calculator).

Otherwise fall through to `pending.md`, then `issues.md`.
